#include "types.h"
#include <stdlib.h>
#include <string.h>

//рeкурсивная функция создания дерева Хафмана. Передаем по указателю значение нового пришедшего элемента. В данном случае, самого нижнего. Также передаем общее количество символов k. Возвращаем - указатель на новую структуру типа symbol
symbol* makeTree(symbol* psym[], int k) {

    // :)
}

//Рекурсивная функция кодирования. На вход приниает указатель на полученный корень дерева
void makeCodes(symbol* root) {

    // :)
}

